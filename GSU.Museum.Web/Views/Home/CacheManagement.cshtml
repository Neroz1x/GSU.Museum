@model IEnumerable<GSU.Museum.Web.Models.HallViewModel>
@{
    ViewData["Title"] = "Управление кешем";
}

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}

@{
    <div class="ml-3">

        <div class="form-check">
            <input class="form-check-input"
                   type="checkbox"
                   value=""
                   id="flexCheckDefault" />
            <label class="form-check-label" for="flexCheckDefault">
                Default checkbox
            </label>
        </div>

        <h3>Выберите данные для кеширования:</h3>
        <div class="form-check ml-3">
            <input class="form-check-input" id="ruText" name="ruText" type="checkbox" value="ru" />
            <label class="form-check-label" for="ruText">Русский текст</label>
        </div>
        <div class="form-check ml-3">
            <input class="form-check-input" id="enText" name="enText" type="checkbox" value="en" />
            <label class="form-check-label" for="enText">Английский текст</label>
        </div>
        <div class="form-check ml-3">
            <input class="form-check-input" id="beText" name="beText" type="checkbox" value="be" />
            <label class="form-check-label" for="beText">Белорусский текст</label>
        </div>
        <div class="form-check ml-3">
            <input class="form-check-input" id="photos" name="photos" type="checkbox" />
            <label class="form-check-label" for="photos">Фотографии</label>
        </div>
        <div class="form-group">
            <button type="button" onclick="CreateCache()" class="btn btn-primary btn-form">
                <svg xmlns="http://www.w3.org/2000/svg" height="1.2em" viewBox="0 0 24 24" width="1.2em">
                    <path fill="currentColor" d="M17 3H5c-1.11 0-2 .9-2 2v14c0 1.1.89 2 2 2h14c1.1 0 2-.9 2-2V7l-4-4zm-5 16c-1.66 0-3-1.34-3-3s1.34-3 3-3 3 1.34 3 3-1.34 3-3 3zm3-10H5V5h10v4z" />
                </svg>Сохранить
            </button>
        </div>
    </div>
}

