<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="GSU.Museum.Shared.Pages.OptionsPage"
             Disappearing="ContentPage_Disappearing"
             >
    <NavigationPage.TitleView>
        <Label Text="{Binding Title}" Style="{StaticResource PageTitle}" />
    </NavigationPage.TitleView>
    <ContentPage.Content>
        <ScrollView >
            <StackLayout Margin="5, 0" Orientation="Vertical">
                <Grid VerticalOptions="Start">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.6"></ColumnDefinition>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>

                    <StackLayout Grid.Column="1" Orientation="Vertical" HorizontalOptions="Start">
                        <Label FontSize="Large" TextColor="Black" Text="{Binding LanguageLabel}"/>
                        <Label FontSize="Medium" Text="{Binding LanguageDescriptionLabel}"/>
                    </StackLayout>

                    <Picker Grid.Column="2" FontSize="Large" 
                            HorizontalOptions="Fill" VerticalOptions="Center" 
                            ItemsSource="{Binding Languages}" 
                            SelectedItem="{Binding SelectedLanguage}" 
                            ItemDisplayBinding="{Binding LanguageName}"/>

                </Grid>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>