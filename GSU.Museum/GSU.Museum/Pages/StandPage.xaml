<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="GSU.Museum.Shared.Pages.StandPage"
             Appearing="ContentPage_Appearing">
    <NavigationPage.TitleView>
        <Label Text="{Binding Title}" FontSize="Large" TextColor="White" />
    </NavigationPage.TitleView>
    <Grid>
        <StackLayout Padding="10">
            <Image HeightRequest="100" Source="{Binding Photo}" IsVisible="{Binding PhotoVisibility}" BackgroundColor="Red"></Image>
            <CollectionView x:Name="ExhibitsCollectionView" SelectionMode="Single" ItemsSource="{Binding Exhibits}"
                        SelectionChangedCommand="{Binding SelectExhibitCommand}"
                        SelectionChangedCommandParameter="{Binding SelectedItem.Id, Source={x:Reference ExhibitsCollectionView}}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout ItemSpacing="10" Orientation="Vertical"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame 
                            HeightRequest="150"
                            Visual="Material"
                            BackgroundColor="LightBlue"
                            Padding="0"
                            Margin="20, 10"
                            HasShadow="True"
                            IsClippedToBounds="True">
                            <Label HorizontalOptions="Center" TextColor="Black" VerticalOptions="Center" FontSize="Medium" Text="{Binding Title}"></Label>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </StackLayout>
        <ActivityIndicator IsVisible="{Binding IsBusy}" 
                           IsRunning="True" 
                           Color="HotPink" 
                           VerticalOptions="CenterAndExpand"/>
    </Grid>
</ContentPage>