<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="GSU.Museum.Shared.Pages.HallPage"
             Appearing="ContentPage_Appearing"
             >
    <NavigationPage.TitleView>
        <Label Text="{Binding Title}" FontSize="Large" TextColor="White" />
    </NavigationPage.TitleView>
    <StackLayout Padding="10">
        <CollectionView x:Name="StandsCollectionView" SelectionMode="Single" ItemsSource="{Binding Stands}"
                        SelectionChangedCommand="{Binding SelectStandCommand}"
                        SelectionChangedCommandParameter="{Binding SelectedItem.Id, Source={x:Reference StandsCollectionView}}">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout ItemSpacing="10" Orientation="Vertical"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame 
                            Style="{StaticResource DefaultMenuItem}">
                        <StackLayout VerticalOptions="Fill">
                            <Label Style="{StaticResource DefaultMenuItemHeader}" Text="{Binding Title}"></Label>
                            <Label Style="{StaticResource DefaultMenuItemBody}" Text="{Binding Description}"></Label>
                        </StackLayout>
                     </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>